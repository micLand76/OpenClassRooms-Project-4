from controllers.base import Controller


if __name__ == "__main__":
    """ to start the program, we call the start method of the controller class """
    controller = Controller()
    controller.start()
